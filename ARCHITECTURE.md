# 🎨 Архитектура SMS Automation с N8N

## 📊 Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────────────┐
│                          N8N WORKFLOW                                │
│                                                                       │
│  ┌────────────┐                                    ┌──────────────┐  │
│  │  Trigger   │──▶ HTTP Request ──▶                │   Webhook    │  │
│  │ (Schedule/ │    POST /webhook/send              │  (получает   │  │
│  │  Manual)   │                                     │   результат) │  │
│  └────────────┘                                    └──────────────┘  │
│       │                                                      ▲        │
│       │                                                      │        │
│       ▼                                                      │        │
└───────┼──────────────────────────────────────────────────────┼───────┘
        │                                                      │
        │  {phone, text, external_id}                         │
        │                                                      │
┌───────▼──────────────────────────────────────────────────────┼───────┐
│                     SMS AUTOMATION SERVICE                   │       │
│                      (Node.js + TypeScript)                  │       │
│                                                              │       │
│  ┌──────────────────────────────────────────────────────┐   │       │
│  │  Express Server (port 3000)                          │   │       │
│  │  POST /webhook/send                                  │   │       │
│  └─────────────────┬────────────────────────────────────┘   │       │
│                    │                                         │       │
│                    ▼                                         │       │
│  ┌──────────────────────────────────────────────────────┐   │       │
│  │  Queue Service                                       │   │       │
│  │  • Управление очередью                              │   │       │
│  │  • Retry логика (до 3 попыток)                      │   │       │
│  │  • Задержки между сообщениями                        │   │       │
│  └─────────────────┬────────────────────────────────────┘   │       │
│                    │                                         │       │
│                    ▼                                         │       │
│  ┌──────────────────────────────────────────────────────┐   │       │
│  │  OpenPhone Service (Playwright)                      │   │       │
│  │  • Автоматизация браузера                           │   │       │
│  │  • Авторизация в OpenPhone                           │   │       │
│  │  • Отправка SMS через UI                            │   │       │
│  │  • Скриншоты при ошибках                            │   │       │
│  └─────────────────┬────────────────────────────────────┘   │       │
│                    │                                         │       │
│           ┌────────┴────────┐                               │       │
│           ▼                 ▼                               │       │
│  ┌────────────────┐  ┌─────────────────────────┐           │       │
│  │   Supabase     │  │  N8N Webhook Service    │           │       │
│  │   (логирование)│  │  🆕 НОВЫЙ СЕРВИС       │───────────┼───────┘
│  │                │  │  • Отправка callback    │           │   ✅ Callback
│  │  • Сохранение  │  │  • Fail-safe подход     │           │   с результатом
│  │    всех попыток│  │  • Подробные логи       │           │
│  │  • История     │  └─────────────────────────┘           │
│  │  • Метрики     │                                         │
│  └────────────────┘                                         │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Последовательность событий

### 1️⃣ Отправка запроса (N8N → Service)

```
N8N
 │
 ├─▶ POST http://localhost:3000/webhook/send
 │   Content-Type: application/json
 │
 │   {
 │     "phone": "+1234567890",
 │     "text": "Hello, World!",
 │     "external_id": "order_12345"
 │   }
 │
 └─▶ Response: { success: true, message: "Added to queue" }
```

### 2️⃣ Обработка очереди (Queue Service)

```
Queue Service
 │
 ├─▶ [попытка 1] OpenPhone.sendMessage()
 │     │
 │     ├─▶ ✅ Успех
 │     │     │
 │     │     ├─▶ Supabase.logMessage(status: "sent")
 │     │     │
 │     │     └─▶ N8nWebhook.sendCallback() ◀── 🆕 НОВОЕ!
 │     │           {
 │     │             "success": true,
 │     │             "status": "sent",
 │     │             "phone": "+1234567890",
 │     │             "external_id": "order_12345",
 │     │             "timestamp": "2026-01-17T10:30:45.123Z",
 │     │             "attempt": 1,
 │     │             "duration_ms": 15234
 │     │           }
 │     │
 │     └─▶ ❌ Ошибка (retryable)
 │           │
 │           ├─▶ Supabase.logMessage(status: "retrying")
 │           │
 │           ├─▶ N8nWebhook.sendCallback() ◀── 🆕 НОВОЕ!
 │           │     { success: false, status: "retrying", ... }
 │           │
 │           └─▶ Wait 2-4 seconds → [попытка 2]
 │
 └─▶ [попытка 2, 3...] (если нужно)
```

### 3️⃣ Получение результата (N8N Webhook)

```
N8N Webhook Node
 │
 ▼
Получает автоматически:
{
  "success": true,
  "status": "sent",
  "phone": "+1234567890",
  "external_id": "order_12345",
  "timestamp": "2026-01-17T10:30:45.123Z",
  "attempt": 1,
  "duration_ms": 15234
}
 │
 ▼
Можно дальше обрабатывать:
 ├─▶ IF success = true
 │     └─▶ Update order status in DB
 │     └─▶ Send notification to Slack
 │
 └─▶ IF success = false
       └─▶ Send alert to team
       └─▶ Log error to monitoring
```

## 🗂️ Структура кода

```
src/
├── config/
│   └── index.ts ────────────────┐
│                                 │  Конфигурация
│                                 │  • OpenPhone credentials
│                                 │  • Supabase config
│                                 │  • N8N webhook URL ◀── 🆕
│                                 │  • Queue settings
│                                 │
├── services/                     │
│   ├── openphone.service.ts ────┤
│   │                             │  Сервисы
│   ├── queue.service.ts ─────────┤  • Бизнес-логика
│   ├── supabase.service.ts ──────┤  • Интеграции
│   └── n8n-webhook.service.ts ◀─┘  • N8N callback ◀── 🆕
│
├── server/
│   └── app.ts ──────────────────┐
│                                 │  Express API
│                                 │  • Webhook endpoints
│                                 │  • Queue management
│                                 │
├── types/                        │
│   └── index.ts ─────────────────┤  TypeScript типы
│                                 │  • MessageStatus
│                                 │  • N8nCallbackPayload ◀── 🆕
│                                 │
├── utils/                        │
│   ├── helpers.ts ───────────────┤  Утилиты
│   └── logger.ts ────────────────┘  • Логирование
│                                    • Helpers
│
└── index.ts ─────────────────────── Главный файл
```

## 🎯 Ключевые компоненты

### N8nWebhookService (🆕 НОВЫЙ)

```typescript
class N8nWebhookService {
  private webhookUrl: string;
  private enabled: boolean;

  async sendCallback(payload: N8nCallbackPayload) {
    // Отправка POST запроса в N8N
    // Fail-safe: ошибки не останавливают SMS
  }
}
```

**Особенности:**
- ✅ Автоматическая отправка callback
- ✅ Fail-safe подход
- ✅ Подробное логирование
- ✅ Маскирование URL в логах для безопасности

### QueueService (обновлен)

```typescript
class QueueService {
  constructor(
    openPhoneService: OpenPhoneService,
    supabaseService: SupabaseService,
    n8nWebhookService: N8nWebhookService  // 🆕
  ) { }

  async processMessage(payload: WebhookPayload) {
    // 1. Отправка SMS
    const result = await openPhoneService.sendMessage();
    
    // 2. Логирование в Supabase
    await supabaseService.logMessage();
    
    // 3. Callback в N8N 🆕
    await n8nWebhookService.sendCallback(result);
  }
}
```

## 📈 Преимущества архитектуры

1. **Модульность**
   - Каждый сервис независим
   - Легко тестировать
   - Легко расширять

2. **Fail-safe**
   - Ошибки N8N callback не ломают SMS
   - Retry механизм для SMS
   - Подробное логирование

3. **TypeScript**
   - Типизация всех данных
   - Автокомплит в IDE
   - Меньше ошибок runtime

4. **Масштабируемость**
   - Очередь с p-queue
   - Легко добавить новые интеграции
   - Docker ready

---

**Готово к использованию! 🚀**
